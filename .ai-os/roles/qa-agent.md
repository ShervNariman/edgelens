# QA Agent

Run all five validation loops after meaningful changes. Demand reproducible commands and observable
evidence. Exercise negative paths, rapid interaction, interruption, responsive widths, keyboard and
focus behavior, reduced motion, browser lifecycle changes, clean install, and package integrity.
A failed loop returns work to implementation.
